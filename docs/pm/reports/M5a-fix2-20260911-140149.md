Completed all four findings.

| Finding and change | Proving tests |
|---|---|
| Configured structlog JSON logging, request IDs, redaction, and CLI initialization | `test_secrets_never_logged`; `test_request_id_propagates_to_logs_and_header` |
| Split routes into six `APIRouter` modules; `app.py` has no route handlers | `test_every_api_route_requires_auth`; 20 router decorators verified |
| Added database-backed ingress URL assertions and spoof protection | `test_ingress_base_path_applied_to_generated_urls`; `test_ingress_trusted_only_in_addon_mode_from_supervisor_ip` |
| Added bounded pre-parser upload guarding for `Content-Length` and chunked bodies | `test_analyze_rejects_oversize_413_while_streaming` |

Additional verification:

- 160 tests passed.
- API modules all meet ≥90% coverage; route modules: analyze 97%, auth 96%, calls 100%, config 91%, recordings 91%, system 100%.
- Pre-commit: passed.
- `just security`: passed.
- `just check`: passed.

Final `just check` tail:

```text
============================ 160 passed in 44.94s =============================
.tools/bin/uv.exe run --project backend python backend/scripts/check_package_coverage.py
.tools/bin/pnpm.cmd --dir web test
...
=============================== Coverage summary ===============================
Statements   : 100% ( 1/1 )
Branches     : 100% ( 0/0 )
Functions    : 100% ( 1/1 )
Lines        : 100% ( 1/1 )
================================================================================
.tools/bin/uv.exe run --project backend python scripts/security_scorecard.py --check
security scorecard: OK
```model=gpt-5.6-luna effort=high resume=01a091df-a553-7a60-8fe0-842299efe1d3
