Completed W1b and marked M6.9 complete in `docs/PROGRESS.md`.

| Claim | Test |
|---|---|
| Backend serves SPA with fallback, CSP, caching, and traversal protection | `backend/tests/integration/test_spa.py` |
| Root and trusted-ingress deep links resolve assets | ADR 0005 and SPA integration tests |
| Real single-port Playwright e2e | `just e2e`: **4 passed (3.4m)**, exit code **0** |
| Teardown cleanup | No listener on port 8790; no ToneWatch `python`/`uv` orphans |
| Required gates | pre-commit, `just api-drift`, `just security`, and final `just check` passed |

Post-teardown check output:

```text
LISTENERS:
TONEWATCH_ORPHANS:
```

The e2e path contains no request or response interception, mocking, rewriting, proxying, or HTML injection. It uses the real backend, generated fixture WAV, real recording file, REST API, and WebSocket.

Final `just check` tail:

```text
================== 254 passed, 2 skipped in 64.03s (0:01:04) ==================
...
Test Files  3 passed (3)
      Tests  38 passed (38)
...
security scorecard: OK
```

`just check` exit code: **0**.model=gpt-5.6-luna effort=high resume=new
