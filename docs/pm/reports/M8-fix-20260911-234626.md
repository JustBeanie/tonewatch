Implemented PM review fixes 1–6.

| Finding | Fix | Proof |
|---|---|---|
| Editable runtime install | Added `--no-editable`; verified copied venv imports package and `web_dist/index.html`. | Local package proof; Docker smoke import step |
| Container e2e lacked source/config | Added shared `scripts/e2e_fixture.py`, used by launcher and CI. | 4 Playwright tests passed; `e2e-container` CI job |
| Compose exposed no port | Added shared `8099:8099` mapping and documentation. | `compose-config` CI job validates rendered ports |
| Recipes were PowerShell-only | Added Unix/Windows recipes, validation, and clear Docker errors. | `just --dry-run` and `just --show` |
| Incorrect action metadata | Corrected exact SHA comments and added offline-safe pin checker. | `just security`; zizmor clean |
| Smoke-test defects | Fixed auth request and PyAV HTTPS verification using certifi options. | Docker smoke job |

Additional changes include licensing ADR/notices for Debian `rtl-sdr`, release-please configuration, GHCR signing, SPDX SBOM attestation, and build provenance.

Required checks passed:

- `pre-commit run --all-files`
- `just api-drift`
- `just security`
- `just check`

`just e2e` ran all 4 tests successfully. The Codex sandbox could not complete Windows `taskkill` cleanup due `Access denied`; forced cleanup left no ToneWatch processes. Docker image build and hardware checks remain `PENDING-CI`.

`just check` tail:

```text
================= 297 passed, 4 skipped in 120.89s (0:02:00) ==================
Required test coverage of 85% reached. Total coverage: 93.63%
Test Files  3 passed (3)
Tests  38 passed (38)
security scorecard: OK
```

The PM should watch these CI jobs on a main push:

- `docker.yml / build-amd64`: image builds and artifact uploads.
- `docker.yml / build-arm64`: arm64 image builds successfully.
- `docker.yml / compose-config`: all compose files validate and publish port 8099.
- `docker.yml / size`: amd64 image is under 350,000,000 bytes.
- `docker.yml / trivy`: no fixable HIGH or CRITICAL vulnerabilities.
- `docker.yml / smoke`: package import, readiness, endpoints, UID 10001, read-only root, and PyAV TLS checks pass.
- `docker.yml / e2e-container`: all four Playwright specs pass using the generated file fixture.
- `release.yml / lint`: release configuration validates.

On a published release, also watch `release.yml / publish`: GHCR receives `X.Y.Z`, `X.Y`, and `latest`, with cosign signing, SPDX SBOM attestation, and build provenance.

M8.1–M8.3 remain `[~] PENDING-CI`; M8.4 remains blocked and untouched.model=gpt-5.6-luna effort=high resume=01a09404-c843-72a2-9b4f-c34f7130980d
