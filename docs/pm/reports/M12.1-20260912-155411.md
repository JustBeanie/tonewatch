Implemented M12.1/M12.3:

- Added MkDocs Material docs group and lockfile updates.
- Added strict site config, public docs pages, internal-doc exclusions, `just docs`, CI docs job, and `site/` ignore.
- Added the required README/site disclaimer.
- Updated progress entries to `[~] PENDING-REVIEW`; Pages publishing remains a user decision.
- No GitHub Pages deployment was configured.

Pages and verification sources:

- [index.md](C:/Users/beanie/Documents/Proj/tonewatch-m121/docs/index.md): README, `__main__.py`, `config/models.py`, `settings.py`.
- Docker: compose files, `docker/Dockerfile`, `settings.py`.
- Raspberry Pi: soundcard/RTL-SDR compose files, `sources/rtlsdr.py`, `config/models.py`.
- Add-on: `settings.py`, `integrations/supervisor.py`.
- Windows: `docs/windows.md`, `__main__.py`, `settings.py`, `service.py`.
- Frequencies/discovery: spectrum UI, `__main__.py`, `dsp/discovery.py`, discovery API.
- Tuning: `dsp/spectrum.py`, `segmenter.py`, `matcher.py`, `config/models.py`.
- Troubleshooting: CLI/API analyzer and Analyze UI.
- Importing: importer, CLI/API routes, Tone Sets UI.
- Home Assistant: MQTT, HA discovery, dispatcher, calls/recordings routes.

Verification:

- `just check` passed.
- `just precommit` passed, including markdownlint, codespell, and clean-room naming.
- `just docs` passed with zero warnings:

```text
INFO - Cleaning site directory
INFO - Building documentation to directory: ...\site
INFO - Documentation built in 0.15 seconds
```

The Windows Playwright launcher hangs only in teardown after every spec passes. All 4 specs passed; `ci-local` was stopped after the teardown stall. Local action-pin verification also reported GitHub unreachable; existing SHA pins were reused and zizmor passed.model=gpt-5.6-luna effort=high resume=new
