Implemented M10b in `ha-addons`:

- Added ToneWatch manifest, docs, README, changelog, translations, tests, and original PNG assets.
- Added `.github/workflows/tonewatch.yml` with pinned actions, manifest/lint/image jobs, and multi-arch checks.
- No commits or pushes made.

Validation:

- Manifest tests: **5 passed**
- `just check`: **passed** in an isolated writable validation copy; 402 backend tests passed, 4 skipped, 44 web tests passed.
- `manifest`: expected pass
- `lint`: expected pass
- `image`: intentionally red until v0.3.0 is released and GHCR is public.

`PENDING-HIL`: real HA installation/startup, ingress UI, Supervisor MQTT credentials and discovery, M11 auto-discovery, Media → tonewatch recordings, hot backup during active monitoring, read-only hardening inspection, and ALSA audio/RTL-SDR capture.model=gpt-5.6-luna effort=high resume=new
