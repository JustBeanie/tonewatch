Implemented W1a shutdown reliability fixes.

| Finding | Fix | Test |
|---|---|---|
| Slow shutdown could lose recordings | Separate finalize/drain budgets; shield encoding and commits; startup orphan reconciliation in `Supervisor.stop`, `CallRecorder.finish`, `PersistenceSubscriber.reconcile_orphans` | `test_shutdown_slow_encoder_still_leaves_consistent_recording_state`, `test_shutdown_never_cancels_inflight_persistence_commit`, `test_startup_reconciles_orphan_recording_files` |
| Orphan files lacked database rows | ADR 0006 policy **(b)** preserves valid files and reconciles them on startup | `test_startup_reconciles_orphan_recording_files` |
| Retention test used proxy polling | Added bounded persisted-row helper | `test_retention_runs_in_app_lifespan` |
| External alert payload path leak | Renamed local attachment plumbing; filesystem paths remain only internal or script argv data | Existing alert tests pass |

Pre-fix failure lines:

```text
E TypeError: create_app() got an unexpected keyword argument 'encoder_factory'
E TypeError: create_app() got an unexpected keyword argument 'shutdown_finalize_timeout_s'
E TimeoutError
```

Repeated test runs:

```text
test_shutdown_slow_encoder_still_leaves_consistent_recording_state: 10/10
test_shutdown_never_cancels_inflight_persistence_commit: 10/10
test_startup_reconciles_orphan_recording_files: 10/10
```

The former wall-clock shutdown test was replaced by these deterministic injected-delay tests. ADR: [0006-shutdown-finalize.md](/C:/Users/beanie/Documents/Proj/tonewatch-hotfix/docs/decisions/0006-shutdown-finalize.md).

Validation passed:

- Pre-commit: all hooks passed
- `just api-drift`: passed
- `just security`: passed
- `just check`: passed
- 247 backend tests passed, 2 skipped
- Pipeline coverage: 95.69%; recording coverage: 96.45%
- No `sys.platform` skipif matches
- No `pragma: no cover` in `backend/src`

Final `just check` tail:

```text
Test Files  3 passed (3)
Tests  38 passed (38)
...
================================================================================
.tools/bin/uv.exe run --project backend python scripts/security_scorecard.py --check
security scorecard: OK
```model=gpt-5.6-luna effort=high resume=01a09301-4daa-7342-ae5f-4ec776a2ca17
