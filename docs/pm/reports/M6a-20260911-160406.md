Implemented M6a (M6.1–M6.4) across the shell, dashboard, calls, tone sets, auth status endpoint, generated API schema, and dependencies.

| Claim | Test |
|---|---|
| Ingress URLs, CSRF, 401 routing | `urls respect ingress base path`; `client sends csrf header on mutations`; `401 redirects to login` |
| WebSocket reconnect/resubscribe | `ws reconnects and resubscribes` |
| Dashboard live feed/meters/health | `dashboard shows live call from ws event`; `level meter is accessible` |
| Calls filters/audio URLs | `calls filters sync to url`; `call detail plays api-provided recording url` |
| Tone-set validation/errors/delete/test | Required named tone-set tests in `web/tests/m6a.test.tsx` |
| Accessibility smoke coverage | `axe: no violations on key pages` |

Verification:

- `just check` — passed.
- Web: 19 tests passed; coverage 90.23% statements, 91.08% lines, 86.74% functions, 80.34% branches.
- Backend: 193 tests passed; 93.43% coverage.
- `just security` — passed.
- `pnpm --dir web build` — passed.
- `git diff --check` — passed.

Remaining PM/environment blockers are recorded in [docs/PROGRESS.md](C:/Users/beanie/Documents/Proj/tonewatch/docs/PROGRESS.md): `just api-drift` detects the expected uncommitted generated OpenAPI changes, and all-file pre-commit cannot start because the local uv Python target link is missing.model=gpt-5.6-luna effort=medium resume=new
