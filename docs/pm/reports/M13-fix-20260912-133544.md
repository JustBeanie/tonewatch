Completed M13-fix after resuming at the interrupted discovery-route coverage work.

| Finding | Fix and verification |
|---|---|
| F1 | Configured, disabled, and source-unassigned tone sets now suppress candidates, including cooldown repeats. Golden tests pass for repeats, disabled sets, and known + unknown pages. |
| F2 | Level gate fails closed with `-inf`; `getattr` fallback removed. Quiet-tone regression passes. |
| F3 | Missing/foreign clips no longer cause 500s; deletion never unlinks another file. API tests pass. |
| F4 | Alert events use a deduplicated `Literal` enum; invalid values fail validation. Schema regenerated. |
| F5 | `since` is normalized to UTC; naive values are explicitly treated as UTC. Offset and naive filtering tests pass. |
| F6 | Added public `AudioEncoder.encode_samples_to_path`; discovery no longer calls `_write`. Recording tests pass. |
| F7 | Final check reports three existing environment skips; none were introduced by M13. |

Conflict files resolved:

- `docs/security/asvs/checklist.csv`: retained applicable rows and rederived exact `path:line` evidence with LF/QUOTE_ALL formatting.
- `docs/security/threat-model/README.md`: retained M10a credential content and added M13 clip/endpoints coverage.
- `web/src/api/openapi.json`
- `web/src/api/generated/schema.d.ts`

The generated API files were resolved by running `just gen-api`.

Validation:

- `just check`: **369 passed, 3 skipped**; DSP 95.89%, pipeline 97.26%; web 42 passed; security scorecard OK.
- `just test-slow`: **1 passed, 371 deselected**; zero false discoveries.
- `just bench`: mean **543.0776 ms**, 600-second audio, approximately **1,105× realtime**; recorded in `docs/benchmarks.md`.
- No conflict markers remain.
- `docs/PROGRESS.md` contains M13.1–M13.7 as `[~] PENDING-REVIEW`, with LF endings.
- No `docs/pm/**` or `.pre-commit-config.yaml` changes.

Skipped tests:

1. Real MQTT broker test: Windows selector-loop limitation.
2. HTTPS certificate test: local PyAV TLS probe unavailable.
3. Token-file permission test: POSIX mode bits unavailable on Windows.

API-drift and e2e remain host-only PM checks per the brief; e2e should use `TONEWATCH_E2E_PORT=8794`.

No commits, fetches, or pushes were made. Goal marked complete; elapsed time was approximately 2h34m.model=gpt-5.6-luna effort=high resume=01a095f9-878c-7753-ae9c-6c338b9e633e
