Rebase resolution is complete with no actual conflict markers, clean `git diff --check`, and no `docs/pm/**` or gate changes.

| Claim | Evidence |
|---|---|
| M9 + M10 CLI/settings/MQTT/DB behavior combined | `just check`: 342 passed, 3 skipped |
| DAST headers and 422 login handling preserved | Full API/security tests passed |
| ASVS evidence refreshed | 19 positive rows uniquely rederived; checklist QUOTE_ALL/LF |
| Security gates pass | Pre-commit, `just security`, scorecard, API drift all passed |
| E2E scenarios pass | 4/4 passed on port 8792 |

Conflict resolutions:

- `__main__.py`: retained M9 `service`/`selftest` commands and M10 `db checkpoint`.
- `settings.py`: retained frozen `%PROGRAMDATA%` default plus add-on options, URL validation, MQTT mode, and password redaction.
- `alerts/mqtt.py`: retained M9 bounded close timeout plus Supervisor credential resolution and source handling.
- `checklist.csv`: retained both rows and recalculated evidence paths.
- `storage/db.py` and `api/app.py` merged cleanly while retaining both branches’ behavior, including Alembic fallback, checkpoint support, DAST headers, 422 login handling, and add-on target creation.

`just check` final tail:

```text
342 passed, 3 skipped
Test Files 3 passed
Tests 38 passed
security scorecard: OK
```

`just --set TONEWATCH_E2E_PORT 8792 ci-local` passed all stages and all four E2E scenarios, then hung only during Windows Playwright teardown after the scenarios completed. The PM should rerun E2E on the host.

CI jobs to watch if the image is rebuilt: Docker size must remain under 350 MB; smoke must start and pass import/readiness checks; Trivy must report no blocking vulnerabilities or configuration findings.

`PENDING-HIL`: real Home Assistant add-on installation and `services: [mqtt:want]` permission, Supervisor MQTT credential rotation/no-service behavior, discovery into Home Assistant, recordings under Media → tonewatch, hot backup using the checkpoint command, and real audio capture per ADR 0009.model=gpt-5.6-luna effort=high resume=01a094ed-af99-73c3-b807-1a3b3018b667
