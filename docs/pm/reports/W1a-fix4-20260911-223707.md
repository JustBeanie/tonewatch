Implemented W1a-fix4.

Root cause was hypothesis 3 plus the test’s runaway fake clock: `CallRecorder.finish()` swallowed cancellation after encoding, so hot reload could never complete; the test advanced simulated realtime without wall-clock pacing. The snapshot and channel-yield contracts were already correct and are now covered by regression tests.

| Finding | Fix | Test |
|---|---|---|
| Cancellation swallowed during finish | `recording/recorder.py:finish` re-raises cancellation after encoding/reset | `test_channel_cancel_ends_task_during_finish` |
| Fake realtime source outran teardown | Integration fake clock sleeps 1 ms per frame | `test_toneset_created_via_api_detects_on_running_channel` |
| Runaway source scheduling | Regression coverage for per-frame yielding | `test_channel_loop_yields_under_runaway_source` |
| Ring snapshots repeated per frame | Regression coverage confirms one snapshot per call start | `test_recorder_per_frame_cost_is_constant_after_call_start` |

Pre-fix failure captured:

```text
E       Failed: DID NOT RAISE CancelledError
```

The hot-reload test passed **20/20 with three CPU burners**. `pytest-timeout` is configured globally at 120 seconds with a 60-second faulthandler timeout.

Gates passed:

- `pre-commit run --all-files`
- `just api-drift`
- `just security`
- `just check`
- Pipeline coverage: 97.46%
- Recording coverage: 96.85%

Final `just check` tail:

```text
Tests  38 passed (38)
...
security scorecard: OK
```model=gpt-5.6-luna effort=high resume=01a09301-4daa-7342-ae5f-4ec776a2ca17
