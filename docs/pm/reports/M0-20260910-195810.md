M0 implementation is in the working tree, but the Definition of Done is not fully met because this execution environment lacks `uv`.

Per task:

- M0.1: Added repo metadata, Apache-2.0 `LICENSE`, README, `.editorconfig`, `.gitattributes`, and `.gitignore`.
- M0.2: Added backend package, `--version` CLI, `pyproject.toml`, and test.
- M0.3: Added Vite React-TS app, strict configs, ESLint, Prettier, Vitest, and passing component test.
- M0.4: Added cross-platform [justfile](/C:/Users/beanie/Documents/Proj/tonewatch/justfile) and pre-commit configuration.
- M0.5: Added CI, CodeQL, PR-title workflow, Renovate, templates, CODEOWNERS, SECURITY, and CONTRIBUTING.
- M0.6: Added audio codec spike and [ADR 0001](/C:/Users/beanie/Documents/Proj/tonewatch/docs/decisions/0001-audio-libs.md). Linux matrix results remain `PENDING-CI`.
- M0.7: Added [scripts/gh-bootstrap.sh](/C:/Users/beanie/Documents/Proj/tonewatch/scripts/gh-bootstrap.sh); it was not run.

Verification:

- `pnpm --dir web install` — passed.
- Web ESLint — passed.
- TypeScript `tsc -b` — passed.
- Vitest with coverage — passed, 100%.
- Prettier check — passed.
- `git diff --check` — passed.
- `just setup` — blocked: `uv` not available.
- `just check` — blocked at Python lint for the same reason.
- `pre-commit run --all-files` and the PyAV spike could not run because they require `uv`.

Deviations: GitHub Actions use tag references with `# TODO(pin-sha)` comments because SHA verification could not be completed. `actionlint`, `hadolint`, and `gitleaks` are documented as CI-only in ADR 0002.

`docs/PROGRESS.md` records M0.1–M0.6 complete and M0.7 waiting on the user.
