Implemented M9-ci-fix2 in four scoped files.

- G1: targeted upgrades for `gzip`, `libpcre2-8-0`, `libsqlite3-0`, `perl-base`; builder-only `binutils`; shared-object stripping; existing test/cache cleanup retained. Estimated impact: ~20 MB saved versus blanket upgrade, ~10 MB added over the previous image, and ~10–25 MB recovered by stripping. Exact image size requires CI Docker.
- G2: bounded 30-second SCM polling for start/stop, complete underscore state names, timeout errors, and detached startup/shutdown logging. Added fake-SCM regression tests.

Verification:

- `just check` passed: `356 passed, 3 skipped`; package coverage gates passed; final tail: `security scorecard: OK`.
- `just ci-local` with `TONEWATCH_E2E_PORT=8798` reached e2e; all 4 specs passed.
- Windows Playwright launcher hangs only in teardown after all specs pass.
- Docker image size, Trivy, multi-arch build, and real Windows SCM lifecycle remain CI-only validations.

No commits or PM-owned files were changed.model=gpt-5.6-luna effort=high resume=new
