Implemented M10a app-side add-on support.

| Claim | Evidence |
|---|---|
| Add-on options, Supervisor MQTT, first boot, redaction, URLs, checkpoint | 20 new unit tests in [test_m10_addon.py](C:/Users/beanie/Documents/Proj/tonewatch-m10a/backend/tests/unit/test_m10_addon.py) |
| Full backend/web validation | `just check`: 328 passed, 4 skipped; 38 web tests passed; scorecard OK |
| Security and hooks | `just security` and pre-commit all files passed |
| API schema | `just api-drift` passed using isolated writable Git metadata |
| Documentation and decisions | [ADR 0009](C:/Users/beanie/Documents/Proj/tonewatch-m10a/docs/decisions/0009-addon-mode.md), Docker README, HIL checklist, threat model, ASVS |

Added Supervisor credential rotation, MQTT runtime modes, first-boot target creation, absolute recording URLs, SQLite WAL checkpoint CLI, API/audit secret masking, security evidence, and the ALSA device-mapping audio decision.

The four Chrome E2E tests all reported `ok`. The Windows Playwright web-server wrapper remains alive during teardown, so `just e2e` does not return cleanly despite passing all tests. This is the only remaining local DoD blocker.

Pending HIL items:

- Confirm `services: [mqtt:want]` is sufficient without `hassio_api`.
- Verify Supervisor MQTT rotation, no-service handling, discovery, and M11 auto-discovery.
- Verify recordings under Media → tonewatch.
- Run hot backup during an active call.
- Verify real ALSA audio capture according to ADR 0009.

No Docker image files changed, so no new docker size, smoke, or trivy checks are required; if the Docker workflow runs, PM should confirm those existing jobs remain green.model=gpt-5.6-luna effort=high resume=new
