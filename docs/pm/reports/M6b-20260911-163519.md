Implemented M6b web UI work:

| Claim | Proving test |
|---|---|
| M6a behavior preserved | Existing 19 M6a tests |
| Spectrum subscription/capture/accessibility | Required spectrum tests |
| Sources CRUD, devices, RTL-SDR conversion, URL validation | Required sources tests |
| Alerts/settings behavior | Required alert tests |
| Analyze validation/rendering | Required analyze tests |
| Web accessibility and coverage | `axe: no violations on m6b pages` |

Web coverage: **90.30% statements, 80.90% branches, 88.03% functions, 91.11% lines**.

`just check`, `just api-drift`, `just security`, and `pnpm --dir web build` pass. Backend: **193 tests passed**.

Added Playwright launcher, tests, and CI job. Local `just e2e` remains blocked because Chromium download timed out in the sandbox. All changes remain uncommitted per `AGENTS.md` local-mode instructions.model=gpt-5.6-luna effort=medium resume=new
