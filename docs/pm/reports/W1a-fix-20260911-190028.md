W1a PM review is complete. The running app now:

- Publishes `RecordingStored` after commit and alerts with API URLs only.
- Restarts channels on tone-set changes, finalizing active calls.
- Finalizes recordings during shutdown cancellation.
- Moves PyAV/stat work to a worker thread.
- Preserves script-only `recording_path` substitution.
- Uses deterministic per-call alert ordering.

| Finding | Fix | Test |
|---|---|---|
| Alert race and path leak | `events.py:RecordingStored`, `persistence.py:_persist`, `dispatcher.py:handle/_payload/_record` | Three new alert tests plus real webhook test |
| Tone-set hot reload | `supervisor.py:reload` restarts channels on tone-set changes | `test_toneset_created_via_api_detects_on_running_channel` |
| Shutdown finalization | `app.py:create_app`, existing `Supervisor.stop`/`Channel.run` lifecycle | `test_app_shutdown_during_post_roll_finalizes_recording_within_timeout` |
| Blocking I/O and polling robustness | `persistence.py:_recording_facts`, Event-based integration tests | Full suite; W1a alert and retention tests |

Pre-fix failure evidence:

- `test_recording_ready_alert_waits_for_persisted_row_without_polling`: `E assert 404 == 200`
- `test_external_alert_payloads_never_contain_filesystem_paths`: `E assert False`
- `test_dispatcher_does_not_swallow_session_errors`: `E AssertionError: assert 'alert attempt persistence failed' in ''`
- `test_toneset_created_via_api_detects_on_running_channel`: `E TimeoutError`
- `test_app_shutdown_during_post_roll_finalizes_recording_within_timeout`: `E TypeError: create_app() got an unexpected keyword argument 'shutdown_timeout_s'`

`docs/PROGRESS.md` now cites the new tests. No golden expectations changed.

Verification passed:

- `pre-commit run --all-files`
- `just api-drift`
- `just security`
- `just check` — exit 0
- 245 backend tests passed, 2 skipped
- `grep -rn "pragma: no cover" backend/src` is empty

Remaining `recording_path` references are internal dispatcher state, local webhook attachment validation, and script-target argv substitution; it is excluded from webhook, MQTT, and HA payloads.

`just check` tail:

```text
dsp: 97.94% (minimum 95.00%)
pipeline: 96.87% (minimum 95.00%)
recording: 97.74% (minimum 90.00%)
alerts: 92.73% (minimum 90.00%)

Test Files 3 passed (3)
Tests 38 passed (38)

=============================== Coverage summary ===============================
Statements   : 90.36% ( 300/332 )
Branches     : 81.69% ( 183/224 )
Functions    : 88.23% ( 105/119 )
Lines        : 91.16% ( 289/317 )
================================================================================
.tools/bin/uv.exe run --project backend python scripts/security_scorecard.py --check
security scorecard: OK
```model=gpt-5.6-luna effort=high resume=01a09301-4daa-7342-ae5f-4ec776a2ca17
