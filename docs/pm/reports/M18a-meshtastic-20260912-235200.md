Implemented M18.1–M18.3 in the working tree.

- Added `MeshtasticTarget`, validation, MQTT sender, URL scrubbing, UTF-8 truncation, rate limiting, coalescing support, dispatcher wiring, and alert-target test endpoint.
- Added ADR 0012, public Meshtastic docs, threat-model evidence, tests, and regenerated OpenAPI client.
- Marked M18.1–M18.3 `[~] PENDING-REVIEW`.

Verified:

- `just check`: **409 passed, 4 skipped**; Vitest **44 passed**; coverage and docs gates passed.
- E2E: **4 specs passed**. Windows launcher then hung during teardown after all specs passed.
- `just ci-local`: all checks passed through security/license validation, then stopped at expected uncommitted generated-client `api-drift`.
- No platform-skipped tests matched the required audit pattern.

Verified protocol requirements: topic `<root>/2/json/mqtt/`, gateway MQTT/JSON enabled, channel named `mqtt` with downlink enabled, payload fields `from`, `to`, `channel`, `type: "sendtext"`, and string `payload`; 200-byte text cap. Sources: [official MQTT documentation](https://meshtastic.org/docs/software/integrations/mqtt/), [message limits](https://meshtastic.org/docs/software/android/user/messages-and-channels.md), [firmware MQTT source](https://github.com/meshtastic/firmware/blob/develop/src/mqtt/MQTT.cpp). The GPL-3.0 Python library/protobufs are not shipped; MQTT JSON keeps ToneWatch’s Apache-2.0 implementation independent.

Example payload:

```json
{"from":2596014644,"to":4294967295,"channel":1,"type":"sendtext","payload":"TONE FIRE EMS 12:34"}
```

Changed files:

`backend/src/tonewatch/{config/models.py,alerts/dispatcher.py,alerts/ha_discovery.py,alerts/meshtastic.py,api/routes/config.py}`, `backend/tests/unit/test_meshtastic.py`, `docs/decisions/0012-meshtastic-transport.md`, `docs/guide/meshtastic.md`, `docs/security/{threat-model/README.md,asvs/checklist.csv}`, `docs/PROGRESS.md`, `mkdocs.yml`, and generated `web/src/api/{openapi.json,generated/schema.d.ts}`.model=gpt-5.6-luna effort=medium resume=new
