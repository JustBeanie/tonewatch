Implemented M14a live restream backend and security docs. No commits or pushes made.

Verification:

- `just check`: passed
  - Pytest: `413 passed, 4 skipped`
  - Vitest: `5 passed, 44 passed`
- `just ci-local`: all substantive gates passed; all 4 Playwright specs passed. Windows Playwright launcher hangs only in teardown after every spec passes.
- `just gen-api` completed; API drift verified clean using an isolated temporary Git index.
- PyAV spike passed. Encoder: 0.928 ms p50, 1.501 ms p95, 7.437 ms max per 100 ms frame; ~26 ms codec priming latency.
- Token format: `v1.<expiry>.<nonce>.<signature>`, HMAC-SHA256 over `live|<source_id>|<expiry>|<nonce>`, dedicated `live_stream_secret`; verification uses expiry, source scope, and `hmac.compare_digest`. Rotation invalidation tested.
- Log capture proves tokens do not appear in application or Uvicorn access logs.
- Progress entries M14.1–M14.3 and M14.6 are `[~] PENDING-REVIEW`.

Changed files:

`backend/src/tonewatch/__main__.py`, `api/app.py`, `api/deps.py`, `api/routes/config.py`, `api/routes/live.py`, `api/routes/ws.py`, `config/models.py`, `events.py`, `importers/tones_cfg.py`, `integrations/supervisor.py`, `pipeline/channel.py`, `pipeline/supervisor.py`, `service.py`

`backend/src/tonewatch/streaming/__init__.py`, `streaming/live.py`

`backend/tests/integration/test_m14a_live_stream.py`, `tests/spike/test_live_encoding.py`, `tests/unit/test_live_stream.py`, `tests/unit/test_s4_security.py`

`docs/PROGRESS.md`, `docs/decisions/0011-live-stream-encoding.md`, `docs/guide/listen-live.md`, `docs/security/asvs/checklist.csv`, `docs/security/threat-model/README.md`, `docs/security/threat-model/tonewatch.json`, `mkdocs.yml`

`web/src/api/openapi.json`, `web/src/api/generated/schema.d.ts`model=gpt-5.6-luna effort=medium resume=01a09946-e736-7fa1-9590-1600485bdc8c
