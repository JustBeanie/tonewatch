Implemented M15.1–M15.4 backend changes:

- Added bounded O(1) histogram-based squelch estimator.
- Added hysteresis, attack, hang, noise-floor mode, config validation, and tests.
- Added source config migration to `rtl_fm_squelch`.
- Added pipeline state, `SquelchChanged`, watchdog handling, optional recording stop.
- Added WS `squelch_open` (`null` when off).
- Added API `squelch_open` and `last_activity_at`.
- Added MQTT/HA activity discovery and state publishing.
- Regenerated OpenAPI client.
- Added tuning documentation and progress markers.

`just check` passed earlier:

- Pytest: **407 passed, 4 skipped**
- Vitest: **44 passed**
- DSP coverage: **95.96%**
- Pipeline coverage: **96.15%**

`ci-local` passed through tests and checks, but failed api-drift because generated API files are intentionally uncommitted in this engineer worktree. The final rerun later hung in Windows web lint teardown.

Changed files include the new `dsp/squelch.py`, squelch tests, config/events/pipeline/watchdog/alerts/API/RTL source files, tuning docs, progress log, generated API files, and the RTL integration fixture.model=gpt-5.6-luna effort=medium resume=new
