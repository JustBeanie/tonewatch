S4 review findings are fixed; final `just check` is green.

| Finding | Fix | Proving test |
|---|---|---|
| 0. Production stream settings | `supervisor.py` passes settings through `Channel` to `StreamAudioSource` | `test_stream_block_private_setting_reaches_running_source` |
| 1. Redirect SSRF | Python `httpx` redirect resolution, per-hop URL validation, FFmpeg `max_redirects=0` | `test_ffmpeg_redirect_option_is_effective`, `test_stream_redirect_to_blocked_host_refused`, `test_stream_redirect_to_allowed_host_followed_and_decoded` |
| 2. IP pinning/SNI | ADR-0005 selects hostname preservation and DNS revalidation on reconnect | `test_stream_sends_original_host_header`, `test_stream_revalidates_dns_on_every_reconnect`, `test_stream_https_certificate_verification` |
| 3. FFmpeg protocol abuse | `protocol_whitelist` blocks `file:` even when validation is bypassed | `test_file_scheme_exploit_regression`, `test_ffmpeg_protocol_whitelist_blocks_file_read_even_if_validation_bypassed` |
| 4. Hollow ASVS evidence | 253 exact source rows, row-specific evidence, 190 honest GAP entries, strengthened validator | Five `test_asvs_*` tests |
| 5. Real audio hardware in tests | Autouse `sounddevice` guard and patched integration source | `test_tests_cannot_open_real_audio_devices` |

Pre-fix evidence included `Failed: DID NOT RAISE Exception` for FFmpeg redirect handling; the protocol exploit independently read the temporary token before whitelist enforcement.

ASVS summary:

| Chapters | Disposition |
|---|---|
| V1–V5 | Controls recorded individually; remaining gaps target S5 |
| V6–V10 | Authentication/session gaps target M9; unsupported flows are N/A |
| V11–V13 | Crypto/config controls recorded; TLS limitation accepted under AR-004; gaps target M10 |
| V14 | Recording access/retention controls recorded; remaining gaps target M12 |
| V15–V16 | Secure-coding/logging controls recorded; remaining gaps target S6 |

The complete auditable breakdown is in [checklist.csv](C:/Users/beanie/Documents/Proj/tonewatch-s4/docs/security/asvs/checklist.csv). `GAP-001` through `GAP-190` are registered in [gaps.md](C:/Users/beanie/Documents/Proj/tonewatch-s4/docs/security/gaps.md), each with requirement, rationale, and target milestone.

Accepted risks:

- AR-001: Windows token-file ACL enforcement — expires 2026-11-30.
- AR-002: Zeroconf metadata disclosure — expires 2026-11-30.
- AR-003: DNS-rebinding TOCTOU window — expires 2026-11-30.
- AR-004: Windows PyAV HTTPS verification probe limitation — expires 2026-11-30.

Pinning decision and TLS probe: [ADR-0005](C:/Users/beanie/Documents/Proj/tonewatch-s4/docs/decisions/0005-stream-url-pinning.md).

SAMM was rescored with new Requirements-driven Testing, Security Testing, and Defect Management evidence; scorecards were regenerated.

Final verification:

```text
================== 276 passed, 3 skipped in 74.29s (0:01:14) ==================
...
alerts: 92.76% (minimum 90.00%)
...
tools/bin/uv.exe run --project backend python scripts/security_scorecard.py --check
security scorecard: OK
```model=gpt-5.6-luna effort=high resume=01a09301-931e-7490-9ebc-7295a51f3432
