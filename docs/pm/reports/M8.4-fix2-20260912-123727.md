Completed M8.4-fix2.

| Finding | Resolution | Tests |
|---|---|---|
| Truncated streamed imports could apply | Oversize streams return `http.disconnect`; 413 is sent once, downstream responses are suppressed, and `ClientDisconnect` is contained. | Raw and multipart `apply=true&mode=replace` tests verify unchanged app state, YAML, and audit rows. |
| Path variants could bypass the guard | Added a guarded trailing-slash alias and root-path normalization. | Trailing-slash and `/prefix` root-path tests pass. |
| Malformed lengths | 400 for invalid/negative/empty values; 413 for oversized values. | Five-case parametrized regression passes. |

No conflict markers remain. The sole conflicted file, `__main__.py`, retains main’s self-test/service commands and the importer CLI.

Final `just check` passed:

```text
338 passed, 3 skipped
Test Files  4 passed (4)
Tests       40 passed (40)
security scorecard: OK
```

Private test pass line:

```text
1 passed, 6 deselected in 0.68s
```

`api-drift` and e2e remain explicitly host-only for PM execution. No `docs/pm/**` files were edited. Goal marked complete; recorded usage was 945,530 tokens over approximately 1 hour 51 minutes.model=gpt-5.6-luna effort=high resume=01a095b3-0944-7c51-93b3-1304d3ef77ac
