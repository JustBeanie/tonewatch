M12.5 is complete. No `docs/pm/**` files were edited.

Implemented:

- Renamed importer, route, tests, fixtures, identifiers, CLI token, UI copy, and generated API client.
- Added Alembic migration `0005_rename_tones_cfg_audit_event.py`.
- Added clean-room `pygrep` pre-commit hook and CONTRIBUTING guidance.
- Updated PLAN, PROGRESS, ASVS, threat model, and security evidence.
- Private fixture lookup now prefers `fixtures/private/tones_cfg/tones.cfg`, with fallback to the old local directory. PM should move the fixture.

Inventory:

- Before: 25 content matches, 5 matching filenames.
- After: 0 exact content matches outside `docs/pm/`; 0 matching filenames.
- One unrelated lockfile substring contains `Tdo`, but it is not a word-boundary match.

Rename map includes:

`tones-cfg.py` → `tones_cfg.py`; `import_tones_cfg.py` → `import_tones_cfg.py`; `/api/import/tones-cfg` → `/api/import/tones-cfg`; `import tones-cfg` → `import tones-cfg`; `tones_cfg_import` → `tones_cfg_import`.

Verification:

- `just check`: passed; 399 backend tests passed, 3 skips, 44 web tests passed.
- `just precommit`: all hooks passed.
- `just ci-local` with port 8799: API drift passed; all 4 e2e tests passed.
- Private fixture: `1 passed, 6 deselected in 0.47s`.

No `feat!`/BREAKING marker is needed because the importer was not exposed in v0.2.0.model=gpt-5.6-luna effort=high resume=new
