Implemented W1a composition-root wiring.

| Finding | Fix | Test |
|---|---|---|
| Recorder absent | Wired `CallRecorder`, reset per call, finalize on EOF/errors/shutdown | Three recording tests |
| Watchdog disconnected | Injected watchdog into `Channel` | `test_healthy_feed_stays_healthy_through_supervisor` |
| Retention absent | Started configured `RetentionService` in lifespan | `test_retention_runs_in_app_lifespan` |
| Hook errors swallowed | Added typed `RecorderHook`; removed `TypeError` fallback | `test_recorder_hook_type_error_propagates` |
| Alerts lacked served URL | Resolve persisted recording IDs to `/api/recordings/{id}` | `test_webhook_alert_fires_for_real_recorded_call` |

All 8 required tests now pass. Pre-fix failure lines included:

- `test_app_records_call_end_to_end`: `E assert (1 == 1 and 0 == 1)`
- `test_source_eof_during_post_roll_finalizes_recording`: `E assert (1 == 1 and 0 == 1)`
- `test_sequential_calls_on_one_channel_produce_two_recordings`: `E assert (2 == 2 and 0 == 2)`
- `test_healthy_feed_stays_healthy_through_supervisor`: `E TypeError: create_app() got an unexpected keyword argument 'sleep'`
- `test_retention_runs_in_app_lifespan`: `E assert rows == [] and not old_path.exists()`
- `test_webhook_alert_fires_for_real_recorded_call`: `E RuntimeError: coroutine raised StopIteration`
- `test_recorder_hook_type_error_propagates`: `E Failed: DID NOT RAISE <class 'TypeError'>`
- `test_channel_finalizes_recording_on_source_error`: `E assert 0 == 1`

Validation passed:

- `just check`: 240 backend tests passed, 2 skipped; 38 web tests passed.
- `pre-commit run --all-files`: passed.
- `just api-drift`: passed.
- `just security`: passed.
- No `pragma: no cover` matches in `backend/src`.
- Added `W1: Wiring` with `W1a.1`–`W1a.6` to `docs/PROGRESS.md`.

The webhook test uses a test-only monkeypatch of `resolve_and_validate` for `127.0.0.1`; production loopback blocking remains enforced at `backend/src/tonewatch/alerts/urlsafety.py:99`.model=gpt-5.6-luna effort=high resume=new
