M0 is locally verified and corrected. M0.6 remains `[~]` because Ubuntu amd64/arm CI cannot run before the M0.7 GitHub gate.

Changes completed:

- M0.1–M0.5 ticked in `docs/PROGRESS.md`.
- Added `backend/README.md`; fixed Hatchling readme path.
- Added `.python-version` and Python 3.13.13 compatibility.
- Removed `uv python install` from setup.
- Added PowerShell `-ExecutionPolicy Bypass`.
- Added Ruff format checking and Prettier `--check`.
- Added local Windows-compatible pre-commit hooks and documented manual CI-only skips in ADR 0002.
- Resolved all GitHub Action tags to verified commit SHAs.
- Recorded Windows PyAV results in ADR 0001.
- `scripts/gh-bootstrap.sh` remains unrun.

Verification tails:

```text
just setup
...
Done in 690ms using pnpm v10.34.5
```

```text
just check
...
2 passed in 0.49s
...
1 passed
Statements: 100%
Lines: 100%
```

```text
uv run --project backend pre-commit run --all-files
...
end-of-file-fixer........................................................Passed
trailing-whitespace......................................................Passed
codespell................................................................Passed
```

Audio spike:

```text
PyAV version=18.1.0
FFmpeg: libavutil 60.26.102, libavcodec 62.28.102, libavformat 62.12.102
PASSED
1 passed in 0.49s
```

Open follow-up: run the configured M0.6 spike on Ubuntu amd64 and arm64 once the GitHub repository/CI exists.
