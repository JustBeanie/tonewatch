Implemented M8.1–M8.3:

- Added pinned multi-stage [Dockerfile](C:/Users/beanie/Documents/Proj/tonewatch/docker/Dockerfile), `.dockerignore`, Compose examples, and Docker README.
- Added hardened multi-arch Docker CI with size, Trivy, smoke, HTTPS/PyAV, and container e2e checks.
- Added release-please config and GHCR signing, SPDX SBOM, and provenance workflow.
- Updated licensing policy, notices, ADR 0004, `just` Docker recipes, and Playwright external URL support.
- M8.1–M8.3 recorded as `[~] PENDING-CI`; M8.4 remains blocked.

Verification:

- `just check` passed last: **297 passed, 4 skipped; 93.53% coverage; 38 web tests passed; security scorecard OK**.
- `just security` passed.
- `just api-drift` passed.
- All-files pre-commit passed.
- Local Playwright assertions: 4 passed. Docker checks remain CI-only because Docker is unavailable locally.

CI jobs for the PM to watch:

| Job | Pass condition |
|---|---|
| `docker.yml: build-amd64` | amd64 image builds |
| `docker.yml: build-arm64` | arm64 image builds |
| `docker.yml: size` | image is under 350 MB |
| `docker.yml: trivy` | no HIGH/CRITICAL fixable vulnerabilities |
| `docker.yml: smoke` | probes, UID 10001, read-only root, and HTTPS PyAV test pass |
| `docker.yml: e2e-container` | all Playwright specs pass against the container |
| `release.yml: lint` | release configuration validates on push |

Also verify the published-release jobs complete GHCR push, keyless cosign signing, SPDX SBOM attestation, and build provenance.model=gpt-5.6-luna effort=medium resume=new
