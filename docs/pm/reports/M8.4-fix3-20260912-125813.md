Implemented M8.4-fix3.

- Fixed truncation: crossing chunks return `http.disconnect`; apply, YAML, supervisor state, and audit remain unchanged.
- Tightened middleware matching to exact `/api/import/ttd`, with trailing slash and root-path support.
- Added raw and multipart three-chunk regressions.
- Resolved conflicts in `__main__.py`, ASVS checklist, and both threat-model files, preserving M10a and importer behavior. Importer threat entry is `TM-029`.
- Refreshed ASVS line evidence from unique snippets.
- No conflict markers remain.

Red evidence before the fix:

```text
assert uploaded.status_code == 413
E assert 200 == 413
```

Claim → test:

| Claim | Verification |
|---|---|
| Chunked oversized imports cannot apply | `test_streamed_oversized_apply_does_not_persist_raw_or_multipart` |
| Route matching is exact | `test_ttd_guard_covers_trailing_slash_and_root_path` |
| ASVS evidence is referential | 9 ASVS tests passed |
| Full checks pass | `just check`: 358 passed, 3 skipped |
| Private fixture passed | `1 passed, 6 deselected in 0.47s` |

Pre-commit and `just security` also passed. `api-drift` and e2e remain host-only per the fix3 brief.model=gpt-5.6-luna effort=high resume=01a095b3-0944-7c51-93b3-1304d3ef77ac
