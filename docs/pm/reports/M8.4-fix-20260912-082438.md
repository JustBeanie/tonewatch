Implemented all three M8.4-fix findings.

| Finding | Fix | Regression |
|---|---|---|
| Malformed `Content-Length` | Strict parsing returns 400 for `abc`, `-5`, `1.5`, and empty values; oversized values return 413. | Parametrized API test covers all five cases. |
| Private `request._receive` usage | Replaced the importer guard with pure ASGI byte-counting middleware. | Declared, streamed, multipart, and file-size tests pass. |
| Fixture gaps | Added CRLF equivalence coverage and asserted the missing-tolerance default/note. | Importer fixture tests pass. |

The only conflicted file was [backend/src/tonewatch/__main__.py](C:/Users/beanie/Documents/Proj/tonewatch-m84/backend/src/tonewatch/__main__.py). I retained main’s `selftest` and Windows service commands/parser/dispatch, alongside the legacy tones.cfg importer CLI parser and dispatch. No conflict markers remain.

Validation:

- Final `just check`: passed — 336 backend tests, 3 skips; 40 web tests; scorecard OK.
- `just ci-local` passed pre-commit, lint, typecheck, tests, security, and scorecard.
- Private fixture:

```text
1 passed, 6 deselected in 0.50s
```

Two host-environment blockers remain:

- `api-drift`: `just gen-api` succeeds, but `git diff --exit-code -- web/src/api` fails because generated files cannot be staged in the read-only worktree Git metadata.
- E2E on port 8796: all four scenarios passed, but the Windows Playwright launcher exits 1 during teardown. The PM can rerun these two gates on the host.

The goal remains active pending those host-only gates.model=gpt-5.6-luna effort=high resume=01a095b3-0944-7c51-93b3-1304d3ef77ac
