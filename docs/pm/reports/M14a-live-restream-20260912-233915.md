M14a is not complete. I implemented the initial backend/docs slice, but verification is blocked by the missing managed CPython 3.13.13 runtime and Node dependencies.

Changed files:

- Live hub, MP3 encoder, token signing: `backend/src/tonewatch/streaming/live.py`
- Live API routes: `backend/src/tonewatch/api/routes/live.py`
- App/channel/supervisor wiring and listener WS events
- `AppConfig.live_stream` and `SourceBase.live_stream_enabled`
- Config preservation and source listener counts
- Spike test and unit tests
- ADR: [0011-live-stream-encoding.md](C:\Users\beanie\Documents\Proj\tonewatch-m14a\docs\decisions\0011-live-stream-encoding.md)
- Public guide: `docs/guide/listen-live.md`
- Threat model JSON/README
- `mkdocs.yml`
- `docs/PROGRESS.md` markers

Token format:

`v1.<expiry>.<nonce>.<signature>`

The signature is HMAC-SHA256 over `live|<source_id>|<expiry>|<nonce>` using the dedicated `live_stream_secret` file. Verification occurs in `api/routes/live.py` via `verify_live_token`; rotation invalidates previous tokens. Request logging records only the URL path, never the query string. Threat-model JSON parses successfully, and `git diff --check` passes.

Verification status:

- `just check`: blocked before tests by missing CPython target.
- `just ci-local` with port 8804: blocked during pre-commit for the same Python issue; frontend tools are also missing.
- No pytest or vitest summary lines are available.
- `just gen-api`: blocked by the missing Python runtime, so the generated client was not regenerated.
- Encoder CPU/latency measurements could not run; the ADR records the intended codec behavior and priming latency, but not host measurements.model=gpt-5.6-luna effort=medium resume=new
