Implemented and verified the core M15a fixes:

- Independent software/hardware squelch watchdog inputs.
- Correct legacy RTL-SDR migration, preserving dict configs.
- Supervisor-less source status returns `null`.
- Preserved RMS semantics and added `squelch_level_dbfs`.
- Regenerated API client.
- Added regression tests.

Verification:

- `just check`: passed.
- Pytest: 412 passed, 4 skipped.
- Vitest: 44 passed.
- DSP coverage: 95.87%.
- Pipeline coverage: 95.88%.
- Pre-commit, lint, typecheck, security, and docs passed.

Remaining blockers:

- `ci-local` stopped at expected API drift because generated files are uncommitted; `git diff --exit-code -- web/src/api` showed the generated schema changes.
- E2E could not complete: all four specs failed at navigation with `Cannot navigate to invalid URL`.
- The brief’s full mandatory test matrix and red-first evidence for every numbered fix are not fully satisfied, so I cannot truthfully mark the goal complete.model=gpt-5.6-luna effort=medium resume=new
