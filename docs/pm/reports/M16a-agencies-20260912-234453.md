Implemented M16a backend foundations and verified:

- `just check`: passed — pytest `407 passed, 4 skipped`; Vitest `44 passed`; coverage `88.3%`.
- `ci-local TONEWATCH_E2E_PORT=8805`: passed through security/docs checks, but stopped at API-drift because generated files are uncommitted. Staging them was blocked by `.git/worktrees/.../index.lock` permission denied.
- M16.1–M16.3 marked `[~] PENDING-REVIEW`.
- Added agency models, validation, CRUD/GeoJSON API, migration `0005`, call snapshots/filtering, event agency payloads, map config/CSP, docs, security model entries, tests, and regenerated API clients.

Field limits and GeoJSON rules are documented in `docs/guide/agencies.md`; CSP tests cover empty, exact-origin, and injection cases.

No commits were made.model=gpt-5.6-luna effort=medium resume=new
