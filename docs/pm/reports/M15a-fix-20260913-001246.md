The M15a-fix goal remains incomplete.

Completed:

- Fixed watchdog separation so software squelch off still detects flatlines.
- Preserved RTL-SDR hardware squelch independently.
- Fixed legacy migration to preserve dict-valued software squelch.
- Added null-safe API source status handling.
- Restored RMS semantics and added `squelch_level_dbfs`.
- Replaced the floor estimator with an O(1) fixed histogram.
- Added source status tracking and regenerated API artifacts.

Verification:

- Previous `just check`: **407 passed, 4 skipped**; Vitest **44 passed**; DSP **95.96%**; pipeline **96.15%**.
- `ci-local` passed tests, static checks, coverage, Vitest, security, and docs, then failed at api-drift because generated files are uncommitted:
  `git diff --exit-code -- web/src/api`
- A final `just check` rerun stalled in Windows web lint after ruff completed.

Remaining required work includes the red-first watchdog regression tests, Hypothesis properties, golden burst test, channel detection-through-closed-squelch test, recording persistence test, MQTT clearing tests, and complete final verification.model=gpt-5.6-luna effort=medium resume=01a09946-d288-7cd0-949c-ce4e872e0688
