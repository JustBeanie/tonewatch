Implemented S5 in the working tree. No `docs/pm/**` files were changed.

| Requirement | Change | Proof |
|---|---|---|
| 1. DAST | Added `docker.yml / dast`, authenticated ZAP baseline/API scans, pinned ZAP digest, reports, and [`docker/zap/rules.tsv`](C:/Users/beanie/Documents/Proj/tonewatch/docker/zap/rules.tsv). | YAML/pre-commit pass. Docker/ZAP execution is `PENDING-CI`. |
| 2. Hardening | Added [`test_docker_hardening.py`](C:/Users/beanie/Documents/Proj/tonewatch/backend/tests/unit/test_docker_hardening.py), Trivy scan for `docker/`, and [`docs/hil-checklist.md`](C:/Users/beanie/Documents/Proj/tonewatch/docs/hil-checklist.md). | Hardening test passes in `just check`; Trivy is `PENDING-CI`. |
| 3. ASVS | Added resilient snippets, vendored SHA verification, SameSite coverage, CSP hardening, and V3 gap conversions. | ASVS tests pass; scorecard passes. |
| 4. Action pins | Re-pinned all workflows and added `security.yml / action-pins`; checker now handles action subpaths and globs. | 82 references covered; `just security` passes locally with expected offline notice. |
| 5. SAMM/DSOMM | Updated DAST/container evidence and regenerated both scorecards honestly. | `security_scorecard.py --check` passes. |
| 6. Threat model | Updated TM-013/TM-024 evidence and threat-model JSON. | Threat-model tests pass. |

Converted ASVS gap rows:

- **V3.4.3** — snippet `response.headers["Content-Security-Policy"] = (\n        "default-src 'none'; object-src 'none'; base-uri 'none'; frame-ancestors 'self'"`
  — test `test_security_headers_present`.
- **V3.5.1** — snippet `def write_auth(request: Request) -> None:`
  — test `test_csrf_required_for_cookie_state_changes`.

ZAP rules contain no `WARN` or `IGNORE` overrides, so there are no override justifications. Configured rules are explicit `FAIL` entries for IDs `10017, 10020, 10023, 10024, 10025, 10038, 10054, 10055, 90019, 90020, 90021, 90022, 40012, 40014, 40016, 40018`, following the documented ZAP rules-file format. [ZAP baseline configuration](https://www.zaproxy.org/docs/docker/baseline-scan/)

The PM should watch:

- `docker.yml / dast`: both ZAP scans exit 0 and four HTML/JSON reports upload.
- `security.yml / action-pins`: prints `action pins verified: 82 reference(s)`.
- `security.yml / trivy-config`: both repository and `docker/` scans exit 0.

`docs/PROGRESS.md` remains `[~] S5 — PENDING-CI` as required.

`pre-commit run --all-files`, `just api-drift`, and `just security` passed. Chrome E2E ran all four scenarios successfully, but the Windows Playwright launcher did not terminate after the tests, so the command could not exit cleanly and was stopped. Docker/ZAP/Trivy remain CI-only in this environment.

Final `just check` passed:

```text
================= 308 passed, 4 skipped in 111.19s (0:01:51) ==================
...
Test Files  3 passed (3)
Tests       38 passed (38)
...
Coverage summary
Statements   : 90.22%
...
.tools/bin/uv.exe run --project backend python backend/scripts/security_scorecard.py --check
security scorecard: OK
```model=gpt-5.6-luna effort=high resume=new
