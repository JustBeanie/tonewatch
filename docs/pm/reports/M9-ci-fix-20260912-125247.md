Implemented F1–F3.

| Failure | Fix | Verification |
|---|---|---|
| F1 service crash | Disabled uvicorn logging config; detached stdio logs to `<data_dir>/tonewatch.log`. | Windows unit regressions pass in `just check`. |
| F2 runtime export | Added strict marker-aware validator and workflow integration. | `just runtime-export-check` passes real lockfile; validator tests pass. |
| F3 Debian CVEs | Added `apt-get upgrade -y` to the existing runtime APT layer and documented removal criteria. | Docker/Trivy unavailable locally. |

Also added `just windows-smoke`; frozen build and smoke pass:

- Version: `0.2.0`
- Devices JSON valid
- `fixture-page` detected
- 65-module import walk passed

`just check` passed: 331 backend tests, 38 web tests, coverage and scorecard green.

`ci-local` reached all checks, API drift, and all four e2e specs on port 8797, but the Windows Playwright launcher hung during post-test teardown in this sandbox and was interrupted. Real Windows SCM lifecycle, Docker build/size/Trivy, and manual actionlint/hadolint binaries require CI verification.

No action pins were changed. No files under `docs/pm/` were edited.model=gpt-5.6-luna effort=high resume=new
