M9 implementation is in place and the final `just check` passes, but the Definition of done is not fully met because `just e2e`’s four Chrome tests pass yet its Windows web-server teardown never exits in the sandbox.

| Claim | Evidence |
|---|---|
| PyInstaller onedir packaging | `just windows-build`; frozen smoke: `0.1.0`, valid devices, `fixture-page`, 65 imports |
| Frozen data-dir behavior | 6 Windows unit tests |
| Service wrapper and graceful stop seam | Fake-manager tests pass; CI lifecycle remains pending |
| MQTT selector thread | Windows real broker test passes |
| Licensing/security | Pre-commit, `just security`, and license check pass |
| API artifacts | `just api-drift` passes |
| Overall checks | `just check`: 315 passed, 3 skipped, 90.94% coverage, web tests 38 passed |

Added:

- PyInstaller spec, Windows service wrapper, CLI self-tests, frozen data-dir logic.
- `windows.yml` with build, runtime-export, smoke, service-lifecycle, and HTTPS jobs.
- Windows install documentation and ADR 0008.
- pywin32 licensing notice.
- M9 progress entries marked `[~] PENDING-CI`.

PM should watch:

- `windows.yml / build`: onedir zip uploads successfully.
- `windows.yml / runtime-export`: Linux runtime export excludes packaging dependencies.
- `windows.yml / smoke`: frozen executable commands pass.
- `windows.yml / service-lifecycle`: service starts, reaches `/readyz`, stops cleanly, unlocks SQLite, and uninstalls.
- `windows.yml / https`: schannel TLS verification reaches the expected `InvalidDataError`.
- `ci.yml / test-python (windows-latest)`: selector-thread MQTT round trip passes.

All workflow actions reuse existing pins; no new SHAs were introduced:

- checkout `fbc6f399...` `# v5.1.0`
- setup-uv `d0cc045d...` `# v6.8.0`
- pnpm/action-setup `fc06bc12...` `# v5.0.0`
- setup-node `a0853c24...` `# v5.0.0`
- download-artifact `d3f86a10...` `# v4.3.0`
- upload-artifact `ea165f8d...` `# v4.6.2`

I did not edit `docs/pm/**`; concurrent PM-owned changes remain untouched.model=gpt-5.6-luna effort=high resume=new
