Implemented M8-fix2 within scope. No `docs/pm/**` files, `.pre-commit-config.yaml`, gitleaks config, or gates were changed.

| Finding | Root cause | Fix | Proof |
|---|---|---|---|
| Runtime image crashed on `httpx` | `httpx` was dev-only; `certifi` was absent | Moved `httpx` to runtime dependencies, added `certifi`, regenerated `uv.lock` | Old no-dev env failed with `ModuleNotFoundError: httpx`; corrected env imported all 64 modules; new `runtime-closure` CI job |
| Alembic import failure | Migration environment called Alembic’s proxy during ordinary module discovery | Made migration import safe and added regression coverage | Storage tests pass; runtime import walk passes |
| Trivy HIGH findings | Base image contained unused pip/setuptools/wheel/ensurepip files | Removed them in the same runtime cleanup layer | Source import scan finds no use; CI `trivy` must pass |
| Image over budget | Baseline was 353,979,998 bytes | Removed base packaging tooling without raising the 350,000,000-byte limit | `size` job asserts `<350000000` and prints `docker history` |
| Startup failure | Missing runtime dependency closure | Added clean startup proof with copied non-editable venv, fresh data directory, and read-only `HOME` | `/readyz` returned 200 locally; smoke and container e2e enforce this in CI |
| Gitleaks finding | PM-handled false positive in release workflow | Left the exact PM fingerprint handling unchanged | CI `gitleaks` remains the final proof |

The measured pre-cleanup image was **353,979,998 bytes / 353.98 MB**. Largest diagnostic layers were approximately:

- `/opt/venv`: 218 MB
- Python base: 48 MB
- APT runtime packages: 9.5 MB

The final CI gate remains strictly **under 350,000,000 bytes**. Docker is unavailable locally, so final size and Trivy results are `PENDING-CI`.

The PM should watch:

- `runtime-closure`: clean runtime-only environment imports every `tonewatch.*` module.
- `size`: exact image size is below 350,000,000 bytes.
- `trivy`: no fixable HIGH or CRITICAL vulnerabilities.
- `smoke`: package imports, `/readyz`, `/`, auth status, `/healthz`, UID 10001, read-only root, and PyAV TLS verification pass.
- `e2e-container`: fixture-backed container starts and all four Playwright specs pass.
- `gitleaks`: no findings.

Local checks passed:

- `pre-commit run --all-files`
- `just api-drift`
- `just security`
- `just check`

Chrome e2e ran all four tests successfully, but the Windows Codex sandbox hangs during Playwright’s final process-tree cleanup because `taskkill` receives `Access denied`; after forced cleanup, no ToneWatch processes remained. This is the remaining local environment blocker.

Final `just check` tail:

```text
================= 304 passed, 4 skipped in 112.69s (0:01:52) ==================
Test Files  3 passed (3)
Tests  38 passed (38)
security scorecard: OK
```model=gpt-5.6-luna effort=high resume=01a09404-c843-72a2-9b4f-c34f7130980d
