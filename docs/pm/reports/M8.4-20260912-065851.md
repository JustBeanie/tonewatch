Implemented M8.4 in the working tree:

| Claim | Verification |
|---|---|
| TTD parser, mapping, limits, redaction | 322 backend tests passed |
| API auth, CSRF, preview/apply/replace, 413 | Integration tests passed |
| CLI and OpenAPI client | `just gen-api` passed |
| Web import preview and Replace confirmation | 40 Vitest tests passed |
| Security documentation and ASVS evidence | `just security` and pre-commit passed |
| Private fixture | `1 passed, 6 deselected in 0.45s` |
| Final repository checks | `just check` passed |

Main files: [ttd.py](C:/Users/beanie/Documents/Proj/tonewatch-m84/backend/src/tonewatch/importers/ttd.py), [import_ttd.py](C:/Users/beanie/Documents/Proj/tonewatch-m84/backend/src/tonewatch/api/routes/import_ttd.py), [ToneSets.tsx](C:/Users/beanie/Documents/Proj/tonewatch-m84/web/src/features/tonesets/ToneSets.tsx).

Additional decisions: missing tolerance uses the model default with a note; nonpositive gaps are omitted; incomplete tone pairs fail only their section; zero successful imports never apply; multipart request envelopes allow framing overhead while file content remains capped at 256 KiB; CLI exit codes distinguish total failure from read/apply errors.

M8.4 is marked `[~] PENDING-REVIEW` in [PROGRESS.md](C:/Users/beanie/Documents/Proj/tonewatch-m84/docs/PROGRESS.md).

Two gates remain environment-blocked:

- `just api-drift` cannot create the worktree index/object data because `.git` is read-only; generated API files are present.
- Windows Playwright runs reached four passing scenarios once, but teardown hung; subsequent retries did not exit 0. No source changes were made to weaken either gate.

Final `just check` tail:

```text
Test Files  4 passed (4)
Tests       40 passed (40)
security scorecard: OK
```model=gpt-5.6-luna effort=high resume=new
