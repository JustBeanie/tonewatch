# Brief: M12.1 + M12.3 — documentation site and README disclaimer

**From:** PM (Claude) · **To:** Codex engineer · **Model:** gpt-5.6-luna, high
**Work dir:** worktree `tonewatch-m121` (branch `m121`, based on `origin/main`). **E2E port:** `TONEWATCH_E2E_PORT=8801`.

## Goal
Build the user documentation site described in `PLAN.md` M12.1, and finish the README disclaimer from M12.3. The site must build locally and in CI. **Do not enable or configure GitHub Pages deployment.** Publishing is a repository setting the user decides on; add only a CI job that *builds* the site with `--strict`.

## Required
1. **Tooling**
   - Add `mkdocs-material` (and only the plugins you need) to a `docs` dependency group in `backend/pyproject.toml`, pinned through `uv.lock`.
   - Add a `mkdocs.yml` at the repo root with `strict: true`, `site_name: ToneWatch`, and navigation matching the pages below. Keep the existing `docs/` layout; don't move security, decision or PM docs into the site. Exclude `docs/pm/`, `docs/security/` and `docs/decisions/` from the nav and build, or use `exclude_docs`, so internal docs are not published.
   - Add a `just docs` recipe that runs `mkdocs build --strict` into a gitignored `site/` directory. Add it to `just check` **only if** it runs offline and in under ~30 s; otherwise add it to `ci-local`, and say which you chose.
   - Add a `docs` job to `ci.yml` that builds with `--strict` on PRs and main pushes that touch `docs/**`, `mkdocs.yml` or `README.md`. Follow the existing workflow conventions:
     - SHA-pinned actions, verified with `just security` (action pins)
     - `permissions: {contents: read}`
     - `timeout-minutes`
     - `persist-credentials: false`
     - the existing concurrency and paths patterns
2. **Pages.** Base every command, setting and default on the actual code (`settings.py`, `config/models.py`, `__main__.py`, the compose files and `docs/windows.md`). Don't invent flags.
   - `index.md`: what ToneWatch does, the supplemental-tool disclaimer, and links onward.
   - Install guides:
     - Docker (amd64)
     - Raspberry Pi (arm64, sound card and RTL-SDR device passthrough, the `dvb_usb_rtl28xxu` blacklist note)
     - Home Assistant add-on (mark it clearly as *coming soon*; the add-on is not published yet)
     - Windows native, linking or adapting `docs/windows.md`
   - Finding tone frequencies: the live spectrum and "capture tone" workflow, and `tonewatch analyze --discover` plus the Discovered tones page.
   - Tuning: `purity_min`, `level_min_dbfs`, `tol_pct`, `min_s`/`max_s`, `max_gap_s`, `cooldown_s`, with guidance on symptoms (missed page vs false positive).
   - Troubleshooting missed pages with `tonewatch analyze file.wav --config config.yaml`, including how to read the segment output.
   - Importing a legacy `tones.cfg` file: the CLI `tonewatch import tones-cfg` and the UI preview/apply, what is and isn't imported, and email and command keys being dropped. Use neutral wording only; the clean-room pre-commit hook enforces it.
   - Home Assistant recipes: MQTT discovery entities and events, `recording_url`, and playing a recording on a `media_player`. Label anything that depends on the future custom integration (M11) as coming soon.
3. **M12.3 README disclaimer.** Keep the existing "supplemental notification tool, not a certified primary alerting system" sentence and add, in plain wording, that recording or rebroadcasting radio traffic may be regulated where the user lives and they are responsible for complying. Put the same disclaimer on the site's `index.md`.
4. **Link hygiene.** `--strict` must pass with no broken internal links or nav warnings. Don't link to external product sites for the legacy format.

## Standing rules
- Never edit `docs/pm/**`.
- Never weaken a gate: no strict-mode off, no skips, no excluded checks.
- Never print or commit anything from `backend/tests/fixtures/private/`.
- Never fetch beyond dependency installation, and never commit or push.
- Don't bump unrelated pins.

## Definition of done
- `just docs` builds with `--strict` and zero warnings. Paste the tail.
- `just ci-local` with `TONEWATCH_E2E_PORT=8801` passes, including pre-commit (clean-room naming, markdownlint, codespell), security (action pins for the new job) and api-drift. If the Windows Playwright launcher hangs only in teardown after every spec passes, say so exactly.
- In `docs/PROGRESS.md`, M12.1 and M12.3 are marked `[~] PENDING-REVIEW` with a one-line note each. Pages publishing is noted as a user decision.
- The final report lists every page with the code files it was verified against, and states that no Pages deployment was configured.
