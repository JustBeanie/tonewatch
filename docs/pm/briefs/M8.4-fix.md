# Brief: M8.4-fix — rebase conflicts + review findings

**From:** PM (Claude) · **To:** Codex engineer · **Model:** gpt-5.6-luna, high (resume of your M8.4 thread)
**Work dir:** worktree `tonewatch-m84` (branch `m84`)

## Situation
PM review of M8.4 **passed**:
- privacy (no real TTD values anywhere outside the gitignored private dir)
- the detection proof
- email redaction (counts only, key names only)
- command keys dropped, never turned into script targets
- the Hypothesis parser test
- the structural-only private test
- the API auth/CSRF/preview/apply/replace/413 tests

Three findings must be fixed before landing, listed under "Required" below.

The PM has already rebased this worktree onto current `main` with `git stash push -u` → `git merge --ff-only origin/main` → `git stash pop`. Since your base, `main` gained:
- **S5 DAST fixes in `api/app.py`:** login returns 422 on malformed bodies; the SPA CSP adds `form-action`/`base-uri`/`object-src`; responses get Permissions-Policy, CORP, COOP and COEP headers; ASVS evidence lines moved.
- **A 0.2.0 version bump and release workflow fix.**
- **A looping `FileAudioSource` fix:** stream time no longer rewinds, which likely also caused your e2e live-recording timeout.
- **The CI diet** and `just ci-local`.
- **M9 (Windows native).**

Any file with `<<<<<<<` markers is a rebase conflict for you to resolve first. Keep both sides' behaviour. In `api/app.py`, keep every new security header and the 422 login handling alongside your import size guard.

## Required
1. **Resolve rebase conflicts.** Re-derive ASVS `path:line` evidence for every positive row from its unique snippet (`docs/security/asvs/checklist.csv`, QUOTE_ALL + LF); don't change statuses or snippets except where your own code moved. In `docs/PROGRESS.md`, keep main's lines (S5 `[x]`, the M13 section, M9 `[~]`) plus your M8.4 line.
2. **Finding 1: a malformed `Content-Length` crashes the import route with a 500.** PM probe against `POST /api/import/ttd`:
   - `Content-Length: abc` → **500** (unguarded `int()`). This is the same bug class the S5 ZAP scan found in login.
   - `Content-Length: -5` → **200**.
   - An oversized value correctly returns 413.

   **Fix:** a non-integer or negative `Content-Length` returns **400** with a generic detail and never a 500. Add a parametrized regression test for `abc`, `-5`, `1.5`, an empty value and a huge value, asserting the exact status codes.
3. **Finding 2: the size guard patches `request._receive`,** a private Starlette attribute that can break on upgrade. Replace it with a supported mechanism: a small pure-ASGI wrapper that counts body bytes from `receive` for that route and answers 413 once over the limit, or a route-level bounded streaming read via `request.stream()`. Keep the existing behaviour and tests:
   - declared oversized → 413
   - streamed oversized without `Content-Length` → 413
   - multipart framing allowance
   - file content capped at 256 KiB
4. **Finding 3: fixture gaps against the brief.** `backend/tests/fixtures/ttd/synthetic.cfg` has no **CRLF** line endings and no section **missing `tone_tolerance`**. Add a committed synthetic CRLF fixture, or build the CRLF variant in the test from the same text with `\r\n`, and assert it parses identically. Add a section with no tolerance, and assert the model default applies plus the "tone_tolerance missing" note. Use invented names and frequencies only; never copy anything from the private fixture.

## Standing rules
- Never edit `docs/pm/**`.
- Never weaken a gate: no skips, deselects, exclusions or lowered coverage.
- Never print or commit anything from `backend/tests/fixtures/private/`.
- Don't fetch, commit or push; the PM does git network operations and commits.

## Definition of done
- No conflict markers remain: `rg -n "^(<<<<<<<|=======|>>>>>>>)" backend web docs scripts .github` finds nothing.
- `just ci-local` exits 0 with `TONEWATCH_E2E_PORT=8796`. Paste the tail. If `api-drift` or e2e is blocked by the sandbox, say so exactly; the PM re-runs them on the host.
- The private fixture test ran and passed (not skipped); paste only its pass line.
- The final message maps each finding to its fix and regression test, and lists each conflicted file with how you combined both sides.
