# Brief: M8.4-fix3 — the truncation bug is still live (+ rebase onto M10a)

**From:** PM (Claude) · **To:** Codex engineer · **Model:** gpt-5.6-luna, high (resume of your M8.4 thread)
**Work dir:** worktree `tonewatch-m84`

## Situation
The PM reviewed M8.4-fix2. The trailing-slash and root-path handling and the `ClientDisconnect` containment are fine. **The blocking finding from fix2 is not fixed.**

In `_TtdBodyLimitMiddleware.limited_receive` (`backend/src/tonewatch/api/app.py`), the branch that runs when a chunk pushes `received` over the limit still does this:

```python
rejected = True
await _send_json(send, 413, "upload too large")
return {"type": "http.request", "body": b"", "more_body": False}
```

That is still a fake clean end-of-body on the crossing chunk. Only *later* `receive()` calls return `http.disconnect`. `request.body()` stops at `more_body: False` and never makes a later call, so the route still gets every chunk received **before** the crossing chunk. A multi-chunk stream of a valid config therefore still parses and applies in replace mode, while the client sees 413.

Your new tests passed without exercising the bug. They most likely sent the oversized body as a single chunk, so the truncated prefix was empty, parsed to zero tone sets, and `apply` was a no-op.

## Required
1. **Write the test first and prove it fails on the current code.**
   - Stream at least 3 chunks without `Content-Length`: the first chunk(s) are a complete valid synthetic config with ≥1 tone set, totalling under the limit, and a later chunk pushes the total over.
   - Use `apply=true&mode=replace`, with a starting config that has a different tone set.
   - Assert 413.
   - Assert the app state config, the saved YAML and the supervisor config are unchanged.
   - Assert no `ttd_import` audit row exists.
   - Add a multipart variant with the same chunking.
   - Paste the red failure output from the current code in your report.
2. **Fix it.** The crossing chunk must return `{"type": "http.disconnect"}` (or raise), never a successful end-of-body. Then check the whole flow: 413 sent exactly once, no double `http.response.start`, no 500, and no leaked app response.
3. **Tighten the path match.** `path.rstrip("/").endswith("/api/import/ttd")` also matches unrelated paths such as `/x/api/import/ttd`. Compare the normalized path against exactly `/api/import/ttd`, trailing slash allowed.
4. **Rebase onto M10a.** The PM will first stash-rebase this worktree onto the new `main` that includes M10a (add-on mode: `settings.py`, `alerts/mqtt.py`, `__main__.py` `db checkpoint`, `storage/db.py`, `api/app.py`, ASVS rows). Resolve any conflict markers while keeping both sides' behaviour, and re-derive ASVS `path:line` evidence for every positive row (QUOTE_ALL, LF).

## Standing rules
- Never edit `docs/pm/**`.
- Never weaken a gate.
- Never print or commit anything from `backend/tests/fixtures/private/`.
- Never fetch, commit or push.

## Definition of done
- The red-then-green evidence for the chunked tests is in the report.
- `just check` passes. Paste the tail.
- The private test passes. Paste only its pass line.
- No conflict markers remain: `rg -n "^(<<<<<<< |=======$|>>>>>>> )" backend web docs scripts .github docker` finds nothing.
