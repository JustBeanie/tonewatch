# Brief: M0 Bootstrap

**From:** PM (Claude) · **To:** Codex engineer · **Model:** gpt-5.6-luna, medium

## Goal
Finish M0.1 through M0.6 from `PLAN.md` so that `just setup` and `just check` both pass on this Windows machine. For M0.7, write `scripts/gh-bootstrap.sh` but **do not run it**.

Read `AGENTS.md` (especially **Local mode**), `PLAN.md` (Linting and quality standards, GitHub setup, M0), and `docs/PROGRESS.md` before starting.

## In scope
M0.1, M0.2, M0.3, M0.4, M0.5, M0.6, and the script half of M0.7. Nothing from M1 or later.

## Environment facts (verified by PM)
- Windows 11. Git for Windows is installed and `core.autocrlf` is on globally. `.gitattributes` in M0.1 must force LF.
- On PATH: `uv`, `node` 24.19 (`C:\Program Files\nodejs`), `pnpm` 10.34 (`%APPDATA%\npm`), `just` 1.58 (`%LOCALAPPDATA%\Microsoft\WinGet\Packages\Casey.Just_Microsoft.Winget.Source_8wekyb3d8bbwe`).
- The `python` on PATH is the Windows Store stub. Use `uv python install 3.13` and `uv`-managed interpreters only.
- There is no GitHub remote and CI cannot run.
- The justfile must work on Windows. Use `set windows-shell := ["pwsh.exe" or "powershell.exe", "-NoLogo", "-NoProfile", "-Command"]` (whichever exists), or keep recipes shell-agnostic. Every recipe must also work on Linux for CI.

## Guidance
- **pre-commit hooks:** prefer wheel-distributed variants that run on Windows without Docker or Go:
  - `shellcheck-py`, `actionlint-py` and `AleksaC/hadolint-py` for shellcheck, actionlint and hadolint.
  - `gitleaks`: use a variant that works on Windows. If none exists, run it only in CI and record that decision in `docs/decisions/0002-precommit-windows.md`.
- **Scaffold only.** Stay minimal: `src/tonewatch/__init__.py` with `__version__` and `__main__.py` with `--version`. Don't build any M1+ code.
- **Web:** a minimal Vite React-TS app with one component test. Configure the strict tsconfig, ESLint flat config and Prettier exactly as `PLAN.md` specifies.
- **Coverage gates:** configure them now, but the tiny scaffold must still pass them (tests exist for `--version`).
- **M0.6 spike:**
  - Write `backend/tests/spike/test_audio_libs.py`. It encodes 1 s of sine to MP3 and to Opus with PyAV, decodes both back, and imports `sounddevice`.
  - Run it locally on Windows.
  - Write `docs/decisions/0001-audio-libs.md` with the Windows result. Linux amd64/arm64 are `PENDING-CI`, and the spike also runs as a job in `ci.yml`.
  - If PyAV can't encode MP3, test the `lameenc` fallback and record the result.
- **GitHub Actions:** pin every action to a full commit SHA with a `# vX.Y.Z` comment. Look the SHAs up online; don't invent them. If you can't verify a SHA, use the tag and add `# TODO(pin-sha)`.
- **Workflows:** set least-privilege `permissions:` on each one. Run `actionlint` on them locally.

## Definition of done
1. `just setup` succeeds from a clean clone state. (You may delete `.venv` and `node_modules` and rerun it to prove this.)
2. `just check` exits 0.
3. `pre-commit run --all-files` exits 0, or every skipped hook is explained in ADR 0002.
4. `docs/PROGRESS.md` ticks M0.1–M0.6. M0.7 stays unticked with a note that the script is ready and waiting on the user.

## Final message format
- **Per task ID:** what you created (key files) and how you verified it (exact commands and results).
- **Deviations** from `PLAN.md`, and why.
- **Risks / follow-ups** and any `BLOCKED:` questions.
